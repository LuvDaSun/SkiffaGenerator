on:
  push:
    tags:
      - ts:oa42-generator@*.*.*

jobs:
  publish:
    uses: ./.github/workflows/publish-ts.yaml
    with:
      NPM_CONFIG_WORKSPACE: oa42-generator
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
