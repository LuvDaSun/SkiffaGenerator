on:
  push:
    branches:
      - "*"
    paths:
      - "*"
      - .github/**
      - fixtures/**
      - packages-ts/oa42-generator/**

jobs:
  test:
    uses: ./.github/workflows/tes-ts.yaml
    with:
      NPM_CONFIG_WORKSPACE: oa42-generator
