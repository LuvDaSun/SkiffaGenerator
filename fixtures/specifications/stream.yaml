openapi: 3.0.1
info:
  title: streaming
  version: v1
paths:
  /live-events:
    get:
      operationId: live-events
      responses:
        "200":
          description: Ok
          content:
            application/x-ndjson:
              schema:
                type: string
  /state:
    get:
      operationId: state
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                type: string
